name: base_core
description: A new Flutter project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  rxdart: ^0.27.0
  logging: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
